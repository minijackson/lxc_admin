<h2>Edit Container</h2>

<%= render "form.html", changeset: @changeset,
                        action: container_path(@conn, :update, @container) %>

<span><%= link "Back", to: container_path(@conn, :index) %></span>
